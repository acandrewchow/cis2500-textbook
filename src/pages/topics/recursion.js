import React from 'react';

const Recursion = () => {
    return (
        <div>
            <h1>Recursion</h1>
            <p>This section will cover topics related to Recursion.</p>
        </div>
    );
};

export default Recursion;