#include <stdio.h>
#include <stdlib.h>

#define SIZE 5  

// TODO: Implement the findClosestToAverage Function
int findClosestToAverage(int arr[], int n) {
    

    return 0;
}

// Do not modify main
int main() {
    int arr[SIZE];
    
    for (int i = 0; i < SIZE; i++) {
        scanf("%d", &arr[i]);
    }

    int closest = findClosestToAverage(arr, SIZE);
    printf("%d\n", closest);
    
    return 0;
}